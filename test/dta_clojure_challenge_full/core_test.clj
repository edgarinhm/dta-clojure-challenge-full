(ns dta-clojure-challenge-full.core-test
  (:require [clojure.test :refer :all]
            [dta-clojure-challenge-full.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
